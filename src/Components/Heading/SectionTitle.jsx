

const SectionTitle = ({title}) => {
    return (
        <div>
            <h2 className="text-[#0077B6] font-bold  md:text-3xl text-center">{title}</h2>
        </div>
    );
};

export default SectionTitle;