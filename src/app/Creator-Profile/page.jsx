import CreatorProfile from "@/Components/CreatorProfile/CreatorProfile";

const page = () => {
    return (
        <div>
            <CreatorProfile></CreatorProfile>
        </div>
    );
};

export default page;