import Notifications from '@/Components/Notifications/Notifications';
import React from 'react';

const page = () => {
    return (
        <div>
            <Notifications />
        </div>
    );
};

export default page;